using UnityEngine;

public class UpdateTimeMainMenu : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        // if(!DBManager.firstLogin && DBManager.remaining_hours > 0)
        //     DBManager.remaining_hours -= Time.deltaTime;
    }
}
